import React, { FC, ReactElement } from "react";

interface Props {

}

const Footer: FC<Props> = (): ReactElement => {
  return (
    <React.Fragment>

    </React.Fragment>
  );
};

export default Footer;